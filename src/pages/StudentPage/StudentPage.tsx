import StudentCard from '@/components/StudentCard/StudentCard';
import { Space } from 'antd';

import type { FC } from 'react';

const StudentPage: FC = () => {
  return <h1 className="pageTitle">Student Name</h1>;
};

export default StudentPage;
